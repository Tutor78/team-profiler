// main function that allows the program to run
const addEmployee = require('./utils/inquirerQuestions');

// main function to initialize the application
function init() {
    addEmployee();
};

init();